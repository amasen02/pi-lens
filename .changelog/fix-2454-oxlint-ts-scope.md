---
section: Fixed
---

- **Extend `npm run lint:js` (oxlint `--deny-warnings`) to `clients/`, `tools/`, `mcp/`, and root `index.ts` TypeScript (refs #2439, #2454)** — `lint:js` blanket-ignored `**/*.ts`/`**/*.tsx` since #2439, so a warning-tier hit in production TypeScript (the bulk of the codebase) shipped past `npm run lint`; a standalone `npx oxlint clients tools` run found 30+ un-triaged warnings at the time. The ignore is now scoped to `tests/**` and `cases/**` (test files and the multi-language dispatch-fixture playground — the same "deliberately not production source" category as the existing `tests/fixtures/*-smoke` exclusions) instead of dropped globally; `clients/`, `tools/`, `mcp/`, and `index.ts` are linted now. `no-undef` stays off for `.ts`/`.tsx` (tsc is the source of truth; unchanged from #2439). Found 35 warning-tier hits against the pinned oxlint (34 at first pass with a stale global `oxlint` shadowing the pinned devDependency — a fresh `npm install` in the worktree surfaced a 35th, `eslint/no-unused-vars` on an unused destructured `_`, that the stale binary's older ruleset didn't carry): 33 fixed (`unicorn/no-new-array` → `Array.from({length})` where every index is provably filled before read; `unicorn/prefer-string-starts-ends-with` → `String#startsWith`/`endsWith`, each site checked for the `m`/`i`-flag and anchor semantics that would break equivalence — none present; `unicorn/no-useless-spread` on `[...map.keys()]`/`[...map.values()]` iterated then mutated in the same loop → direct iteration, which is spec-safe for `Map`/`Set` delete-during-iteration; `unicorn/no-useless-fallback-in-spread` → drop the `?? {}`; `unicorn/no-useless-length-check` → drop the redundant `.length === 0` (`Array#every` already returns `true` on empty); `eslint/no-unused-expressions` on two `cond && call()` statements → `if (cond) call()`; `eslint/no-unused-vars` on an unused destructured element → array-hole skip; `eslint/no-control-regex` on 6 genuine control-byte matches (ANSI-escape stripping, a NUL-byte telemetry scrub) → inline `oxlint-disable-next-line` with a reason, matching #2452's precedent) and 2 allow-listed inline (`clients/lsp/edits.ts`, under concurrent edit for the applyWorkspaceEdit mutation-bridge work, refs #2450 — deferred rather than risking a collision). `.oxlintrc.json`'s `no-array-constructor`-style overlap with ast-grep's `no-array-constructor` rule (refs #1718) is pre-existing dispatcher machinery (`LINTER_OVERLAP` in `clients/dispatch/runners/ast-grep-napi.ts`), not touched here — see the PR body's ownership table.
