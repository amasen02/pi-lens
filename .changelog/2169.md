---
section: Fixed
---

- **Bound Svelte and Prisma language-server verification probes (closes #2169)** — use fast `--help` probes instead of transport-blocked `--version` probes. Verification retains at most 64 KiB of child output and records one bounded degradation when a noisy probe exceeds that cap.
